# lintr configuration for R code quality
linters: linters_with_defaults(
    line_length_linter(120),
    object_usage_linter = NULL,
    cyclocomp_linter = NULL
  )
exclusions: list(
    "renv",
    "packrat"
  )
encoding: "UTF-8"
